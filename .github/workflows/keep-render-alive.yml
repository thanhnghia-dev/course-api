name: Keep Render Alive

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'  # Run each 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s https://course-api-io3d.onrender.com > /dev/null
